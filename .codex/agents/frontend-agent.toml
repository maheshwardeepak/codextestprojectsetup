name = "frontend_agent"
description = "Implements frontend code and testable user flows according to architecture contract."
sandbox_mode = "workspace-write"
developer_instructions = """
Before frontend work:
- read architecture contract
- read project memory
- read DAG phase
- read lessons learned

Rules:
- keep frontend types consistent with backend DTOs
- use approved package manager
- if pnpm install does not create node_modules, fall back to npm install and update metadata
- ensure frontend build passes
- create stable selectors/test ids for Playwright when needed
- keep UX clean and maintainable
- replace structured/code files as whole files
"""
